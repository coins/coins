# Client configuration for regtest (local testing)
# Copy to ../client.toml and adjust as needed

# Publisher endpoint (local regtest publisher)
publisher_url = "http://127.0.0.1:8080"

# Client secret key file (relative to project root)
keyfile = ".data/keys/client_sk.hex"
