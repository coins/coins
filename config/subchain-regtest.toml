# Subchain generation configuration for regtest
# Copy to ../subchain.toml and adjust as needed

# Number of pre-signed connector transactions
# More transactions = longer chain = more sub-blocks before exhaustion
count = 10000

# Bitcoin network
network = "regtest"

# Output file path (relative to project root)
output = ".data/subchains/subchain_regtest.bin"
