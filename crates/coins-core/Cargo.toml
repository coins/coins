[package]
name = "coins-core"
version = "0.1.0"
edition = "2024"

[dependencies]
coins-types = { path = "../coins-types" }
coins-crypto = { path = "../coins-crypto" }
sled = "0.34"
bincode = { version = "2.0.1", features = ["serde"] }
thiserror = "1"

[dev-dependencies]
tempfile = "3"
rand = "0.8"
