{
  "project": "Coins Protocol",
  "branchName": "ralph/multi-token-support",
  "description": "Add multi-token support with u16 token_id and per-token balances",
  "userStories": [
    {
      "id": "US-001",
      "title": "Add token_id to Transaction and update size constants",
      "description": "As a developer, I need Transaction to include a token_id field so transfers can specify which token to send.",
      "acceptanceCriteria": [
        "Add `token_id: u16` field to Transaction struct after recipient_pk",
        "Update TX_SIZE constant from 41 to 43",
        "Update COMPACT_TX_SIZE constant from 13 to 15",
        "Add `token_id: u16` field to CompactTransaction struct",
        "Add NATIVE_TOKEN_ID constant (pub const NATIVE_TOKEN_ID: u16 = 0)",
        "Update Transaction::serialize() to include token_id bytes",
        "Update Transaction::message_to_sign() if it uses serialized bytes",
        "cargo check passes"
      ],
      "priority": 1,
      "passes": true,
      "notes": "File: crates/coins-types/src/lib.rs"
    },
    {
      "id": "US-002",
      "title": "Convert Account to multi-token balances",
      "description": "As a developer, I need Account to store multiple token balances instead of a single balance.",
      "acceptanceCriteria": [
        "Change Account.balance: u64 to Account.balances: BTreeMap<u16, u64>",
        "Add impl Account with balance(token_id: u16) -> u64 method that returns 0 if not held",
        "Add native_balance() -> u64 convenience method that calls balance(0)",
        "Import std::collections::BTreeMap at top of file",
        "cargo check passes"
      ],
      "priority": 2,
      "passes": true,
      "notes": "File: crates/coins-types/src/lib.rs. This will cause compilation errors in other crates until they are updated."
    },
    {
      "id": "US-003",
      "title": "Update State for multi-token accounts",
      "description": "As a developer, I need State to create accounts with empty balance maps and calculate per-token supply.",
      "acceptanceCriteria": [
        "Update create_account() to use balances: BTreeMap::new() instead of balance: 0",
        "Add total_supply_of(token_id: u16) method that sums balances for a specific token",
        "Update existing total_supply() to call total_supply_of(NATIVE_TOKEN_ID)",
        "Import NATIVE_TOKEN_ID from coins_types",
        "cargo check passes for coins-core crate"
      ],
      "priority": 3,
      "passes": true,
      "notes": "File: crates/coins-core/src/state.rs"
    },
    {
      "id": "US-004",
      "title": "Update validator for per-token balance checking",
      "description": "As a developer, I need the validator to check balances per token_id and collect fees in native token.",
      "acceptanceCriteria": [
        "Import NATIVE_TOKEN_ID from coins_types",
        "Use acct.balance(tx.token_id) instead of acct.balance for balance checks",
        "For native token transfers: check native_balance >= amount + fee",
        "For non-native token transfers: check token_balance >= amount AND native_balance >= fee",
        "Deduct from sender using acct.balances.insert() for the specific token_id",
        "For non-native tokens, deduct fee from native balance separately",
        "Credit recipient using recv.balances.insert() for tx.token_id",
        "Credit publisher fees to native token only",
        "cargo check passes for coins-core crate"
      ],
      "priority": 4,
      "passes": true,
      "notes": "File: crates/coins-core/src/validator.rs. This is the core logic change."
    },
    {
      "id": "US-005",
      "title": "Update publisher API for token_id",
      "description": "As a developer, I need the publisher API to include token_id in mempool and broadcast responses.",
      "acceptanceCriteria": [
        "Add token_id: u16 field to MempoolTxResponse struct",
        "Add token_id: u16 field to RecentlyBroadcastTxResponse struct",
        "Update get_mempool handler to include tx.token_id in response",
        "Update get_recently_broadcast handler to include tx.token_id in response",
        "cargo check passes for coins-publisher crate"
      ],
      "priority": 5,
      "passes": true,
      "notes": "File: crates/coins-publisher/src/api.rs"
    },
    {
      "id": "US-006",
      "title": "Update indexer for multi-token support",
      "description": "As a developer, I need the indexer to handle accounts with multiple token balances.",
      "acceptanceCriteria": [
        "Verify TransactionWithStatus works with flattened Transaction (token_id comes through automatically)",
        "Update any account serialization to use balances map instead of single balance",
        "Update routes.rs account endpoints to return balances as map in JSON",
        "cargo check passes for coins-indexer crate"
      ],
      "priority": 6,
      "passes": true,
      "notes": "Files: crates/coins-indexer/src/client.rs, crates/coins-indexer/src/api/routes.rs"
    },
    {
      "id": "US-007",
      "title": "Add --token-id flag to CLI client",
      "description": "As a user, I want to specify which token to send using --token-id flag.",
      "acceptanceCriteria": [
        "Add token_id: u16 field to Send command with #[arg(long, default_value = \"0\")]",
        "Update transaction creation to include token_id from CLI argument",
        "Update Balance command to display all token balances from account.balances map",
        "Format balance output as 'Token X: Y sats' for each token, with '(native)' label for token 0",
        "cargo check passes for coins-client crate"
      ],
      "priority": 7,
      "passes": true,
      "notes": "File: crates/coins-client/src/main.rs"
    },
    {
      "id": "US-008",
      "title": "Update explorer API for token_id",
      "description": "As a developer, I need the explorer API to include token_id in pending transactions.",
      "acceptanceCriteria": [
        "Add token_id: u16 field to PendingTransaction struct",
        "Update get_pending_transactions handler to parse token_id from upstream response",
        "Use tx.get(\"token_id\").and_then(|v| v.as_u64()).unwrap_or(0) as u16",
        "cargo check passes for coins-explorer crate"
      ],
      "priority": 8,
      "passes": true,
      "notes": "File: crates/coins-explorer/src/api_simple.rs"
    },
    {
      "id": "US-009",
      "title": "Add Token column to explorer transaction tables",
      "description": "As a user, I want to see which token each transaction uses in the explorer.",
      "acceptanceCriteria": [
        "Add Token column header to account transaction table",
        "Add Token column header to block transaction table",
        "Add Token column header to mempool/pending transactions table",
        "Display token badge for non-native tokens (token_id !== 0)",
        "Native tokens can show empty cell or 'native' badge"
      ],
      "priority": 9,
      "passes": true,
      "notes": "File: crates/coins-explorer/src/web/static/app.js"
    },
    {
      "id": "US-010",
      "title": "Show multi-token balances on account page",
      "description": "As a user, I want to see all my token balances on the account page.",
      "acceptanceCriteria": [
        "Replace single balance display with balances table",
        "Show Token ID and Balance columns",
        "Label token 0 as '(native)'",
        "Handle empty balances gracefully with 'No tokens' message"
      ],
      "priority": 10,
      "passes": true,
      "notes": "File: crates/coins-explorer/src/web/static/app.js"
    },
    {
      "id": "US-011",
      "title": "Run tests and fix any failures",
      "description": "As a developer, I need all tests to pass with the multi-token changes.",
      "acceptanceCriteria": [
        "Run cargo test --all",
        "Fix any test failures related to Transaction struct changes",
        "Fix any test failures related to Account struct changes",
        "All tests pass"
      ],
      "priority": 11,
      "passes": false,
      "notes": "Tests may need updating to include token_id in test transactions and use balances map in test accounts."
    }
  ]
}
